# Backend base URL (no /api suffix). Required for production builds (Netlify, Vercel, etc.).
# Example: https://your-app.onrender.com
VITE_API_URL=https://zenith-4hjz.onrender.com

# Optional: override Socket.IO URL (defaults to same origin as VITE_API_URL)
# VITE_SOCKET_URL=
